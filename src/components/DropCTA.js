export default function DropCTA() {
  return (
    <section className="flex flex-col items-center justify-between gap-3 bg-black sm:flex-row" style={{ padding: "40px 6%" }}>
      <span
        className="text-[11px]"
        style={{ fontFamily: "var(--font-jetbrains), monospace", color: "rgba(255,255,255,0.3)" }}
      >
        ZENJI // AWAKENING_REDACTED
      </span>
      <span
        className="text-[11px]"
        style={{ fontFamily: "var(--font-jetbrains), monospace", color: "rgba(255,255,255,0.3)" }}
      >
        NO RESTOCKS. EVER.
      </span>
    </section>
  );
}
